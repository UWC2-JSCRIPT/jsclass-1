const firstName = "James";
const lastName = "Norton";

const name = firstName + " " + lastName;
const betterWay = `${firstName} ${lastName}`;

const arr = [1, 2, 3];

arr.forEach((item) => {
  var car = "ferrari";
  console.log(item);
});

let age = 47;

console.log(name);
console.log(betterWay);

// const numbers = [1, 2, 3];
// numbers.map(function (num) {
//   return num * 2;
// });

// numbers.map((num) => num * 2);

// numbers.map((num) => {
//   return num * 2;
// });
